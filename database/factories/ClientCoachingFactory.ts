import client_coaching from 'App/Models/client_coaching'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(client_coaching, ({ faker }) => {
  return {
    //
  }
}).build()
